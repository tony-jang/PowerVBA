<UserControl x:Class="PowerVBA.Controls.Design.ErrorToolTipDesign"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PowerVBA.Controls.Design"
             mc:Ignorable="d" 
             Height="auto" d:DesignWidth="250" Background="White">
    <Grid Background="White" Margin="10">
        <Grid.Effect>
            <DropShadowEffect ShadowDepth="0" BlurRadius="8"/>
        </Grid.Effect>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>

        <TextBlock Margin="10,0" Text="1개의 오류" VerticalAlignment="Center" FontWeight="Bold"/>
        <!--원, 사각형, 화살표 등 다양한&#x0a;디자인의 도형을 삽입할 수&#x0a;있습니다.-->
        <TextBlock Grid.Row="1"  Margin="10,8" Text="- Public절 뒤에는 Sub/Function/Type 절만 올 수 있습니다." TextWrapping="Wrap" VerticalAlignment="Center" Foreground="#FF4F4F4F"/>
        <Button Grid.Row="2"/>
    </Grid>
</UserControl>
